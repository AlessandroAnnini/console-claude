# Security

This is an independent project. It is not affiliated with Anthropic.

Do not commit API keys. The MAIN-world stub (`src/page-stub.ts`) must never contain credentials, `fetch(`, or Anthropic identifiers.

To report a vulnerability, open a private GitHub security advisory if the repo has them enabled, otherwise open an issue without pasting secrets.
