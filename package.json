{
  "name": "console-claude",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build && vite build --config vite.stub.config.ts && vite build --config vite.isolated.config.ts",
    "test": "vitest",
    "test:e2e": "npm run build && playwright test",
    "lint": "tsc --noEmit && eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.62.1",
    "@types/chrome": "^0.1.4",
    "@types/node": "^26.4.0",
    "eslint": "^10.9.1",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.68.0",
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  }
}
